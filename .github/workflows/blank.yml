name: Windows-2025 Cloud Computer

on:
  workflow_dispatch:

jobs:
  setup-cloud-pc:
    runs-on: windows-2025
    steps:
      - name: Download AnyViewer
        run: |
          curl -L -o anyviewer.msi https://download.anyviewer.com/anyviewer_setup.msi

      - name: Install AnyViewer
        run: |
          msiexec /i anyviewer.msi /quiet /norestart
          Start-Sleep -s 10

      - name: Configure AnyViewer (Password = bear)
        shell: pwsh
        run: |
          $configPath = "$env:ProgramData\AnyViewer\anyviewer_config.ini"
          if (-Not (Test-Path $env:ProgramData\AnyViewer)) {
              New-Item -ItemType Directory -Path $env:ProgramData\AnyViewer -Force | Out-Null
          }
          # Create config file with password
          Set-Content -Path $configPath -Value "[general]`npassword=bear" -Encoding ASCII

      - name: Start AnyViewer
        run: |
          Start-Process "C:\Program Files (x86)\AnyViewer\AnyViewer.exe"
          Start-Sleep -s 15

      - name: Show Cloud PC Info
        shell: pwsh
        run: |
          Write-Output "💻 Your personal cloud computer is ready!"
          Write-Output "🖥️ Specs:"
          Write-Output "   • OS: Windows Server 2025 (Cloud Edition)"
          Write-Output "   • RAM: 8–16 GB"
          Write-Output "   • Storage: 256 GB (C:) + smaller D: drive"
          Write-Output "   • CPU: AMD EPYC (2–4 vCores)"
          
          # Try to read Device ID from registry
          $deviceId = (Get-ItemProperty "HKLM:\SOFTWARE\AnyViewer").DeviceID 2>$null
          if ($deviceId) {
            Write-Output "🆔 Device ID: $deviceId"
          } else {
            Write-Output "🆔 Device ID: (could not detect, check AnyViewer window)"
          }
          Write-Output "🔑 Password: bear"
          Write-Output "📱 Connect using AnyViewer on your PC or phone."

      - name: Keep Alive for 6 Hours
        shell: pwsh
        run: |
          $hours = 6
          $minutes = $hours * 60
          for ($i = 1; $i -le $minutes; $i++) {
            Write-Output "⏳ [$i/$minutes] Your cloud computer is still running..."
            Start-Sleep -Seconds 60
